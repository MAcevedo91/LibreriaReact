import React from 'react';
import { Container } from 'react-bootstrap';

const Header = () => {
  return (
    <header className="bg-primary text-white text-center py-3">
      <Container>
        <h1>Librería React</h1>
      </Container>
    </header>
  );
};

export default Header;
